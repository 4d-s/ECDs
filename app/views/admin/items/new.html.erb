<h1>新規商品追加画面</h1>
<div>
  	<%= form_for [:admin, @item] do |f| %>
		<br>
		<label>商品名</label>
		<br>
		<%= f.text_field :item_name %>
		<br>
		<label >在庫数</label>
		<br>
		<%= f.text_field :stock %>
		<%= f.submit "追加" %>
	<% end %>
</div>