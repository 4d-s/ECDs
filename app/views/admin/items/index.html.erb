<h1>All Items</h1>

<table border="1">
<tr>
	<th colspan="3">All Items<th>
</tr>

<div>
	<% @items.each do |item| %>

	<%= attachment_image_tag item, :image_id,  size: "100x100", fallback: "no_image.jpg" %>
	<%= item.item_name %>
	<%= item.price %>
	<%= item.stock %>
<% end %>
</div>
</table>

